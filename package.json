{
  "name": "issentropy-rng",
  "version": "1.0.0",
  "description": "ISSentropy - Космический генератор случайных чисел на основе данных МКС",
  "main": "server/index.js",
  "scripts": {
    "start": "node server/index.js",
    "dev": "nodemon server/index.js",
    "client": "cd client-randiss && npm start",
    "client:build": "cd client-randiss && npm run build",
    "install:all": "npm install && cd client-randiss && npm install"
  },
  "keywords": ["rng", "lottery", "entropy", "visualization", "cryptographic"],
  "author": "",
  "license": "MIT",
  "dependencies": {
    "express": "^4.18.2",
    "cors": "^2.8.5",
    "axios": "^1.6.0",
    "dotenv": "^16.3.1",
    "multer": "^1.4.5-lts.1",
    "better-sqlite3": "^9.2.2"
  },
  "devDependencies": {
    "nodemon": "^3.0.1"
  }
}
